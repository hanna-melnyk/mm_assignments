<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %></title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-5">
    <article>
        <h2><%= article.title %></h2>
        <p><%= article.content %></p>
        <i>Posted by: <%= article.username %> on <%= new Date(article.createdAt).toLocaleString() %></i>
    </article>

    <div class="mt-3">
        <a href="/" class="btn btn-primary">Back to Homepage</a>
        <a href="/articles/<%= article._id %>/edit" class="btn btn-secondary">Edit Article</a>
        <form action="/articles/<%= article._id %>?_method=DELETE" method="POST" style="display: inline;">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit" class="btn btn-danger">Delete Article</button>
        </form>
    </div>
</div>

</body>
</html>
